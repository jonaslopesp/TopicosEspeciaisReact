// vamos criar um componente REACT baseado em funções;
// vamos criar uma função que não tem nome e é default -> padrão do componente;
// esta função será chamada toda vez que utilizarmos o componente
// vamos exportar esta função para ser utilizada em outro arquivo

export default function() {

    return "Que legal, é de arrepiar, fiz meu primeiro programa em REACT"

}